<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easyliu.coordinatorlayoutdemos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CoordinatorToolbar.CoordinatorToolbarActivity"></activity>
        <activity
            android:name=".CoordinatorFloatingActionButton.FloatingActionButtonActivity"
            android:label="@string/title_activity_floating_action_button"
            android:theme="@style/AppTheme" />
        <activity android:name=".CollapsingToolbar.CollapsingDemoActivity"></activity>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>